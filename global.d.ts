// import { Locator } from "playwright";

// // declare global {
// //     namespace PlaywrightTest {
// //       interface Matchers<R> {
// //         toHaveAmount(locator: Locator, expected: number, options?: { timeout?: number }): Promise<R>;
// //         toHavePrice(locator: Locator, expected: number, options?: { timeout?: number }): Promise<R>;
// //         toBeSearchButtonVisible(locator: Locator, options?: { timeout: number }): Promise<R>;
// //       }
// //     }
// //   }
  